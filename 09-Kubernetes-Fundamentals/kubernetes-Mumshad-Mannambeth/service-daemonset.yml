apiVersion: v1
kind: Service
metadata:
  name: daemonset-service
  namespace: default
spec:
  selector:
    tier: daemonset
  type: NodePort
  ports:
  - name: 80-tcp
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30010