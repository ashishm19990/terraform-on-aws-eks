apiVersion: v1
kind: Service
metadata:
  name: dashboard-deployment-service
  namespace: default
spec:
  selector:
    app: dashboard-deployment
  type: NodePort
  ports:
  - name: 80-tcp
    protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30030
